using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ElectronValue
{
    public Vector3 position;

    // Start is called before the first frame update
    public ElectronValue(Vector3 pos)
    {
        position = pos;
    }
}
